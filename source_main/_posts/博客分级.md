---
title: 博客分级
date: 2026-05-27 20:05:06
categories:
tags:
---

出于个人需求，对博客进行切分，通过同一个域名下的不同地址导航

例如需要在同一个项目中构建son子页面，且主站和子页面的博文完全分离

1. 新建_config.son.yml文件，修改其中的url, source_dir, public_dir项，将子页面的文件独立存储于public_son文件夹，子页面的博文独立存储于source_son文件夹

2. hexo g --config _config.son.yml，将子网站的文件单独生成在public_son文件夹。

3. 为部署在服务器上，原先的上传路径是在服务器上的git钩子中指定，若想分别上传需要新建一个钩子，为了可拓展性，修改为rsync上传

4. rsync需要在linux中使用，项目通过git重新放到WSL中，webstorm直接读取wsl的文件进行编辑。

5. WSL配置ssh连通服务器，从而通过rsync直接将生成的博客文件推送到服务器。

6. 配置nginx，原先配置不变，仅添加对于/和/son/的导航。



